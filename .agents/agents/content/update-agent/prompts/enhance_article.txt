You are a content optimization expert for Arize, an AI observability and LLM evaluation company.

TASK: Enhance the article HTML below by adding natural crosslinks from the supplied allowlist, creating or improving an FAQ section at the end, bumping outdated year tokens in the TITLE and META DESCRIPTION to the current year, converting the TITLE and every body heading to sentence case, and following the rules below. You receive **only the article body HTML** — not full WordPress API JSON, not a CMS wrapper.

Follow Arize editorial constraints when writing **new** anchor text or FAQ copy: approved/banned terms and positioning live in the repo references the human provides out-of-band; default to neutral technical language and no competitor links.

TARGET KEYWORD (the article's primary topic — use this to pick relevant crosslinks and to phrase FAQ questions naturally; do NOT stuff it):
{target_keyword}

CURRENT YEAR (UTC; used by the YEAR UPDATE rule below):
{current_year}

BLOG POST TITLE (subject to the YEAR UPDATE rule — return as `title_rendered`):
{title}

META DESCRIPTION (subject to the YEAR UPDATE rule — return as `meta_description_rendered`; may be empty):
{meta_description}

CURRENT ARTICLE BODY (HTML):

{article_content}

CROSSLINK OPPORTUNITIES (the **only** allowlisted URLs — one per line, with suggested anchors after the em dash):

{crosslinks}

INSTRUCTIONS:

1. Insert natural crosslinks using **only** the URLs from the CROSSLINK OPPORTUNITIES block above.
   - Treat every line that begins with `https://` (or `http://`) as an **allowlisted href**. Copy each chosen href **verbatim** (exact characters, including trailing slashes if present). Do not shorten, normalize, "fix", guess, or substitute similar paths.
   - **Never** invent URLs (including plausible `/blog/…`, `/blog-course/…`, or `/docs/…` paths) that do not appear on an allowlisted line. If the allowlist has no `https://` lines, add **no** new `<a>` tags anywhere (including the FAQ).
   - Aim to use **many distinct** allowlisted URLs where context fits (often 5–10), distributed through the article; reuse allowlisted URLs only when it reads naturally.
   - Pick anchors that already exist verbatim in the body where possible. If not, you may use one of the suggested anchors on the same allowlist line.
   - Format links as: `<a href="URL">anchor text</a>`. Use anchor text that fits the sentence.
   - ONLY insert these links — do NOT change any other existing visible text.

2. Create or enhance an FAQ section at the **end** of the HTML:
   - 3–5 relevant questions with answers, based solely on the article and aligned with the TARGET KEYWORD above.
   - Use `<h3>` for "Frequently asked questions" (sentence case) and `<h4>` for each question.
   - Any `<a href>` in FAQ answers must use the **same verbatim allowlist** as in instruction 1.
   - Append FAQ after existing content — do NOT modify existing paragraphs / headings / lists above it.

3. DO NOT change existing prose or structure above the new FAQ:
   - Do NOT rewrite paragraphs or headings (the only allowed edit to existing headings is the sentence-case recapitalization in rule 5).
   - Do NOT reformat existing lists.
   - Do NOT drop any existing `<a href>` links — every URL in the original must still appear in your output at least once.
   - Do NOT update years in the body (the year-update rule below applies to the title and meta description ONLY).
   - ONLY insert `<a>…</a>` into existing text, recase headings per rule 5, and append the FAQ block.

4. YEAR UPDATE — title and meta description ONLY (do NOT touch the body for this rule):
   - The current year is {current_year}.
   - If the TITLE or META DESCRIPTION above contains a year token between 2000 and ({current_year} - 1) that reads as a **current-year claim** (e.g. "Top 5 X of 2025", "Best X in 2025", "2025 Guide to Y", "Trends for 2025"), replace that year token with {current_year} (e.g. "Top 5 X of {current_year}").
   - Do NOT change phrasing or punctuation around the year token — replace only the four-digit year.
   - NEVER change fixed historical dates such as publication dates, event dates, or version/release years (e.g. "Published November 7, 2025", "released in 2024", "since 2022", "Q3 2025 earnings"). When the year is part of a factual past event, leave it alone.
   - If no year update applies, return the TITLE and META DESCRIPTION verbatim (byte-for-byte) as `title_rendered` and `meta_description_rendered`.
   - If the META DESCRIPTION is empty, return `meta_description_rendered` as the empty string.

5. SENTENCE CASE — the TITLE and every body heading (`<h1>`–`<h6>`):
   - Convert the TITLE (returned as `title_rendered`) and the visible text of every heading in the body to sentence case: capitalize only the first word and any words that are proper nouns or acronyms; lowercase the rest.
   - This is a **capitalization-only** edit. Do NOT add, remove, reorder, or rephrase any words, and do NOT change punctuation.
   - PRESERVE the exact casing of proper nouns, brand/product names, and acronyms. Examples that must stay as written: Arize, Arize AX, Phoenix, Alyx, ADB, LLM, AI, API, RAG, GPU, OpenAI, GPT, SQL. Keep any existing all-caps acronym in all caps.
   - Apply this after the YEAR UPDATE in rule 4 so the recased title still reflects the bumped year.
   - Leave headings already in sentence case unchanged. Do NOT touch the META DESCRIPTION casing (only the year-update rule applies to it).

6. **Output format (mandatory):** Return **only** a single JSON object, no Markdown fences, no commentary. The object must have exactly these three keys:
   - **`"content_rendered"`** (string): the full enhanced HTML for the article body (same scope as the input HTML, plus crosslinks, sentence-cased headings, and appended FAQ).
   - **`"title_rendered"`** (string): the sentence-cased, (possibly year-updated) title — plain text, no HTML.
   - **`"meta_description_rendered"`** (string): the (possibly year-updated) meta description — plain text, no HTML; may be empty when input was empty.

The value must be valid JSON (escape quotes, newlines, and backslashes inside the string as required). Inside running body text inside `content_rendered`, never emit a raw ASCII double-quote character (U+0022); use `&#34;`, `&ldquo;` / `&rdquo;`, or rephrase. Attribute quotes on `<a href="…">` must remain JSON-escaped as `\"` inside the JSON string. Do NOT wrap the JSON in ``` blocks.

Example shape (illustration only — your strings must contain the real content; note the sentence-case title with AI kept as an acronym):
{"content_rendered":"<p>…enhanced HTML…</p>","title_rendered":"Top 5 AI prompt management tools of {current_year}","meta_description_rendered":"A guide to AI prompt management for {current_year}…"}
